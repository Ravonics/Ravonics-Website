{
  "name": "ravonics-lead-proxy",
  "version": "2026.08.22",
  "description": "Thin Azure Functions proxy in front of the Ravonics D365 Logic App intake flows. Keeps the Logic App SAS callback URLs server-side, verifies Cloudflare Turnstile, rate-limits per IP, and filters obvious spam before forwarding leads.",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "func start",
    "test": "node --test"
  },
  "dependencies": {
    "@azure/functions": "4.16.2"
  },
  "engines": {
    "node": ">=24 <27"
  }
}
